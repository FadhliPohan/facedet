# Training System Requirements
# For GPU-optimized training with NVIDIA RTX GPUs

# Core Data Processing
pandas
matplotlib
numpy
tqdm
Pillow
psutil

# Image Processing
opencv-python==4.10.0.84  # Pinned version - 4.13.x has broken imread
albumentations  # For data augmentation in preprocessing

# Machine Learning - TensorFlow with GPU Support
tensorflow[and-cuda]>=2.15.0  # Includes CUDA libraries automatically
scikit-learn  # For SVM classifiers and metrics

# Optional: For visualization
seaborn

# Notes:
# - TensorFlow with [and-cuda] includes all CUDA dependencies
# - Requires NVIDIA GPU with CUDA 11.8+ support
# - For CPU-only: use 'tensorflow' without [and-cuda]
# - Installation: pip install -r requirements.txt
